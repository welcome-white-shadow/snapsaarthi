{
  "name": "snapsaarthi",
  "private": true,
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "predev": "npm install --force",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.18",
    "prisma": "^5.22.0",
    "turbo": "latest"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "mongodb": "^7.2.0"
  }
}
